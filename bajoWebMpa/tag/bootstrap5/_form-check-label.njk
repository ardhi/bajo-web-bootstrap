{% if attr.label %}
  {% tag 'label',
    classBase='',
    class='form-check-label' + attr.labelClass|d(''),
    style=attr.labelStyle,
    id=attr.labelId,
    for=attr.for|d(attr.id) %}
    {{- attr.label -}}
    {% endtag %}
{% endif %}