{% set variant = attr.variant|ternary(' btn-' + attr.outline|ternary('outline-', '') + attr.variant, '') %}
<{{ attr.tag|d('button') }}
    class="{{ attr.classBase|d('btn') }}{{ variant }}{% if attr.sizing %} btn-{{ attr.sizing }}{% endif %}{% if attr.tag === 'a' and attr.disabled %} disabled{% endif %}{{ attr.class }}"
    style="{{ attr.style }}"
    type="{{ attr.type|d('button') }}"
    {% if attr.tag === 'a' and attr.disabled %}tabindex="-1" aria-disabled="true"{% endif %}
    {% if attr.tag === 'a' %}{% if not attr.disabled %}href="#" {% endif %} role="button"{% endif %}
    {% if attr.tag === 'input' %}value="{{- content|safe -}}"{% endif %}
    {{ attributes|safe -}}
    {{ params|join(' ') -}}>
    {% if attr.tag !== 'input' %}
    {{- content|safe -}}
    </{{ attr.tag|d('button') }}>
    {% endif %}